/**
 * 起動時ペイウォールの再表示間隔（日）。
 *
 * これが無かった頃、非Proユーザーは毎起動でペイウォールに送られていた
 * （実測で1端末あたり最多15回）。✕ で `/(tabs)` に抜けられる作りなので
 * 起動時表示はゲートではなく露出であり、頻度を絞っても機能は失われない。
 *
 * オンボーディング完走直後の表示もカウント対象にすることで、
 * 「入った直後にもう一度ペイウォール」を防いでいる。
 */
export const LAUNCH_PAYWALL_COOLDOWN_DAYS = 7;
